{
  "name": "react-hk-components",
  "description": "React components for Heroku's Purple 3",
  "repository": "https://github.com/heroku/react-hk-components",
  "env": {
    "NPM_CONFIG_PRODUCTION": {
      "description": "Ensure node buildpack leaves dev dependencies",
      "value": "false"
    }
  },
  "stack": "heroku-24",
  "environments": {
    "test": {
      "scripts": {
        "test": "pnpm run test && pnpm run lint"
      }
    }
  }
}
